info:
  name: Listar Candidato
  type: http

http:
  method: GET
  url: http://localhost:3001/api/candidatos
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
