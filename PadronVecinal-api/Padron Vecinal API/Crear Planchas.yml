info:
  name: Crear Planchas
  type: http

http:
  method: POST
  url: http://localhost:3001/api/planchas
  body:
    type: json
    data: |-
      {
        "color": "rojo",
        "nombreFrente": "kupini unida",
        "creadoPorId": 3
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
​