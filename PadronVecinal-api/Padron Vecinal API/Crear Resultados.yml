info:
  name: Crear Resultados
  type: http

http:
  method: POST
  url: http://localhost:3001/api/resultados
  body:
    type: json
    data: |-
      {
        "planchaId": 1,
        "cantidadVotos": 125,
        "registradoPorId": 3
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
