info:
  name: Actualizar Candidato
  type: http

http:
  method: PUT
  url: http://localhost:3001/api/candidatos/1
  body:
    type: json
    data: |-
      {
        "vecinoId": 2
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
